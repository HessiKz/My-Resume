# Design references

Visual direction comps for the portfolio upgrade. Implementation lives in `style.css` and `index.html`.

| File | Section |
|------|---------|
| `hero-section-reference.png` | Hero layout, teal-on-charcoal, pill CTAs |

Add project screenshots to `assets/images/` as named in `data/projects.json` (e.g. `Bitnitex.png`, `GIG-Find.png`) for GitHub Pages.
